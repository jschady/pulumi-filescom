name: filescom-lifecycle-behavior
runtime: yaml
description: The program creates one webhook behavior on a throwaway folder. The stack config gives the name and the path.

config:
  behaviorName:
    type: string
  behaviorPath:
    type: string
  behaviorType:
    type: string

resources:
  probe:
    type: filescom:index/behavior:Behavior
    properties:
      behavior: ${behaviorType}
      path: ${behaviorPath}
      name: ${behaviorName}
      value:
        urls:
          - https://example.com/pulumi-filescom-behavior-probe
        method: POST
        triggers:
          - create
        headers:
          x-pulumi-filescom-probe: v1

outputs:
  behaviorId: ${probe.id}
  behaviorValue: ${probe.value}
