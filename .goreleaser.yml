# Hand-owned. Do not regenerate with ci-mgmt.
version: 2

project_name: pulumi-filescom

builds:
  - id: provider
    dir: provider
    main: ./cmd/pulumi-resource-filescom/
    binary: pulumi-resource-filescom
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s
      - -w
      - -X github.com/jschady/pulumi-filescom/provider/pkg/version.Version={{.Tag}}

archives:
  - id: archive
    ids:
      - provider
    formats:
      - tar.gz
    name_template: "{{ .Binary }}-{{ .Tag }}-{{ .Os }}-{{ .Arch }}"

changelog:
  disable: true

# A tag carrying a prerelease suffix becomes a GitHub prerelease from this same config.
release:
  prerelease: auto
