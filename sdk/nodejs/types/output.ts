// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";

export interface AutomationDefinition {
    /**
     * Directed connections between nodes.
     */
    edges: outputs.AutomationDefinitionEdge[];
    /**
     * Flat list of nodes. Every node requires a unique id and a type, accepts the node-specific config fields documented below, and action or control-flow nodes may set return to true to include their output in the Automation result.
     */
    nodes: outputs.AutomationDefinitionNode[];
    /**
     * Automation definition schema version.
     */
    schemaVersion: number;
}

export interface AutomationDefinitionEdge {
    /**
     * Source node ID.
     */
    from: string;
    /**
     * Named destination input. Omit for the main input.
     */
    input?: string;
    /**
     * Named source output. Omit for the main output.
     */
    output?: string;
    /**
     * Destination node ID.
     */
    to: string;
}

export interface AutomationDefinitionNode {
    agentCompute?: outputs.AutomationDefinitionNodeAgentCompute;
    aggregate?: outputs.AutomationDefinitionNodeAggregate;
    as2Send?: outputs.AutomationDefinitionNodeAs2Send;
    copyFile?: outputs.AutomationDefinitionNodeCopyFile;
    createFolder?: outputs.AutomationDefinitionNodeCreateFolder;
    deleteFile?: outputs.AutomationDefinitionNodeDeleteFile;
    documentConvert?: outputs.AutomationDefinitionNodeDocumentConvert;
    extract?: outputs.AutomationDefinitionNodeExtract;
    filter?: outputs.AutomationDefinitionNodeFilter;
    gpgDecrypt?: outputs.AutomationDefinitionNodeGpgDecrypt;
    gpgEncrypt?: outputs.AutomationDefinitionNodeGpgEncrypt;
    if?: outputs.AutomationDefinitionNodeIf;
    imageConvert?: outputs.AutomationDefinitionNodeImageConvert;
    importFile?: outputs.AutomationDefinitionNodeImportFile;
    join?: outputs.AutomationDefinitionNodeJoin;
    moveFile?: outputs.AutomationDefinitionNodeMoveFile;
    runAutomation?: outputs.AutomationDefinitionNodeRunAutomation;
    runSync?: outputs.AutomationDefinitionNodeRunSync;
    sendEmail?: outputs.AutomationDefinitionNodeSendEmail;
    setMetadata?: outputs.AutomationDefinitionNodeSetMetadata;
    switch?: outputs.AutomationDefinitionNodeSwitch;
    transform?: outputs.AutomationDefinitionNodeTransform;
    triggerAction?: outputs.AutomationDefinitionNodeTriggerAction;
    triggerEmail?: outputs.AutomationDefinitionNodeTriggerEmail;
    triggerManual?: outputs.AutomationDefinitionNodeTriggerManual;
    triggerScheduled?: outputs.AutomationDefinitionNodeTriggerScheduled;
    triggerWebhook?: outputs.AutomationDefinitionNodeTriggerWebhook;
    unzip?: outputs.AutomationDefinitionNodeUnzip;
    wait?: outputs.AutomationDefinitionNodeWait;
    zip?: outputs.AutomationDefinitionNodeZip;
}

export interface AutomationDefinitionNodeAgentCompute {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeAgentComputeConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeAgentComputeConfig {
    arguments?: {[key: string]: string};
    command: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeAgentComputeConfigOnError[];
    remoteServerId: number;
}

export interface AutomationDefinitionNodeAgentComputeConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeAggregate {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeAggregateConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeAggregateConfig {
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeAggregateConfigOnError[];
}

export interface AutomationDefinitionNodeAggregateConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeAs2Send {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeAs2SendConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeAs2SendConfig {
    as2PartnerId: number;
    as2StationId: number;
    as2Subject?: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeAs2SendConfigOnError[];
}

export interface AutomationDefinitionNodeAs2SendConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeCopyFile {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeCopyFileConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeCopyFileConfig {
    alwaysOverwriteSizeMatchingFiles?: boolean;
    destinationReplaceFrom?: string;
    destinationReplaceTo?: string;
    /**
     * One or more destination path templates.
     */
    destinations?: string[];
    excludePattern?: string;
    flattenDestinationStructure?: boolean;
    ignoreLockedFolders?: boolean;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeCopyFileConfigOnError[];
    overwriteFiles?: boolean;
}

export interface AutomationDefinitionNodeCopyFileConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeCreateFolder {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeCreateFolderConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeCreateFolderConfig {
    /**
     * One or more destination path templates.
     */
    destinations: string[];
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeCreateFolderConfigOnError[];
}

export interface AutomationDefinitionNodeCreateFolderConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeDeleteFile {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeDeleteFileConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeDeleteFileConfig {
    excludePattern?: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeDeleteFileConfigOnError[];
}

export interface AutomationDefinitionNodeDeleteFileConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeDocumentConvert {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeDocumentConvertConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeDocumentConvertConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination?: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeDocumentConvertConfigOnError[];
    overwriteFiles?: boolean;
    targetFormat: string;
}

export interface AutomationDefinitionNodeDocumentConvertConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeExtract {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeExtractConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeExtractConfig {
    contentMode?: string;
    includeContent?: boolean;
    includeMetadata?: boolean;
    maxChars?: number;
    maxPages?: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeExtractConfigOnError[];
}

export interface AutomationDefinitionNodeExtractConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeFilter {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeFilterConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeFilterConfig {
    /**
     * A bare Files TransformScript expression, no braces.
     */
    condition: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeFilterConfigOnError[];
}

export interface AutomationDefinitionNodeFilterConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeGpgDecrypt {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeGpgDecryptConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeGpgDecryptConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination?: string;
    gpgKeyId: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeGpgDecryptConfigOnError[];
    overwriteFiles?: boolean;
}

export interface AutomationDefinitionNodeGpgDecryptConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeGpgEncrypt {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeGpgEncryptConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeGpgEncryptConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination?: string;
    gpgKeyId: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeGpgEncryptConfigOnError[];
    overwriteFiles?: boolean;
}

export interface AutomationDefinitionNodeGpgEncryptConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeIf {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeIfConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeIfConfig {
    /**
     * A bare Files TransformScript expression, no braces.
     */
    condition: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeIfConfigOnError[];
}

export interface AutomationDefinitionNodeIfConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeImageConvert {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeImageConvertConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeImageConvertConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination?: string;
    height?: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeImageConvertConfigOnError[];
    overwriteFiles?: boolean;
    targetFormat: string;
    width?: number;
}

export interface AutomationDefinitionNodeImageConvertConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeImportFile {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeImportFileConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeImportFileConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    content?: string;
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination?: string;
    headers?: {[key: string]: string};
    method?: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeImportFileConfigOnError[];
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    url: string;
}

export interface AutomationDefinitionNodeImportFileConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeJoin {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeJoinConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeJoinConfig {
    joinType?: string;
    /**
     * A bare Files TransformScript expression, no braces.
     */
    leftKey: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeJoinConfigOnError[];
    /**
     * A bare Files TransformScript expression, no braces.
     */
    rightKey: string;
}

export interface AutomationDefinitionNodeJoinConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeMoveFile {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeMoveFileConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeMoveFileConfig {
    destinationReplaceFrom?: string;
    destinationReplaceTo?: string;
    /**
     * One or more destination path templates.
     */
    destinations?: string[];
    excludePattern?: string;
    flattenDestinationStructure?: boolean;
    ignoreLockedFolders?: boolean;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeMoveFileConfigOnError[];
    overwriteFiles?: boolean;
}

export interface AutomationDefinitionNodeMoveFileConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeRunAutomation {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeRunAutomationConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeRunAutomationConfig {
    automationId: number;
    automationVersion: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeRunAutomationConfigOnError[];
}

export interface AutomationDefinitionNodeRunAutomationConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeRunSync {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeRunSyncConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeRunSyncConfig {
    legacySyncIds?: number[];
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeRunSyncConfigOnError[];
    syncIds?: number[];
}

export interface AutomationDefinitionNodeRunSyncConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeSendEmail {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeSendEmailConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeSendEmailConfig {
    attachments?: string[];
    bccs?: string[];
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    body?: string;
    ccs?: string[];
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    from?: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeSendEmailConfigOnError[];
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    subject?: string;
    tos: string[];
}

export interface AutomationDefinitionNodeSendEmailConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeSetMetadata {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeSetMetadataConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeSetMetadataConfig {
    metadata: {[key: string]: string};
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeSetMetadataConfigOnError[];
}

export interface AutomationDefinitionNodeSetMetadataConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeSwitch {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeSwitchConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeSwitchConfig {
    /**
     * Switch cases. Each item contains a unique output name and the condition that sends items to it.
     */
    cases: outputs.AutomationDefinitionNodeSwitchConfigCase[];
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeSwitchConfigOnError[];
}

export interface AutomationDefinitionNodeSwitchConfigCase {
    /**
     * A bare Files TransformScript expression, no braces.
     */
    condition: string;
    /**
     * Identifier used to connect nodes and named inputs or outputs.
     */
    name: string;
}

export interface AutomationDefinitionNodeSwitchConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeTransform {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeTransformConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeTransformConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination?: string;
    fts: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeTransformConfigOnError[];
}

export interface AutomationDefinitionNodeTransformConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeTriggerAction {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeTriggerActionConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface AutomationDefinitionNodeTriggerActionConfig {
    excludePattern?: string;
    path?: string;
    pathTimeZone?: string;
    source?: string;
    triggerActions: string[];
}

export interface AutomationDefinitionNodeTriggerEmail {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeTriggerEmailConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface AutomationDefinitionNodeTriggerEmailConfig {
    allowedSenders?: string[];
    excludePattern?: string;
    limit?: number;
    pathTimeZone?: string;
    saveBody?: boolean;
    source?: string;
}

export interface AutomationDefinitionNodeTriggerManual {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeTriggerManualConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface AutomationDefinitionNodeTriggerManualConfig {
    excludePattern?: string;
    limit?: number;
    path?: string;
    pathTimeZone?: string;
    source?: string;
}

export interface AutomationDefinitionNodeTriggerScheduled {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeTriggerScheduledConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface AutomationDefinitionNodeTriggerScheduledConfig {
    excludePattern?: string;
    holidayRegion?: string;
    interval?: string;
    limit?: number;
    path?: string;
    pathTimeZone?: string;
    recurringDay?: number;
    recurringDays?: number[];
    scheduleDaysOfWeeks?: number[];
    scheduleId?: number;
    scheduleTimeZone?: string;
    scheduleTimesOfDays?: string[];
    source?: string;
}

export interface AutomationDefinitionNodeTriggerWebhook {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeTriggerWebhookConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface AutomationDefinitionNodeTriggerWebhookConfig {
    excludePattern?: string;
    limit?: number;
    path?: string;
    pathTimeZone?: string;
    source?: string;
}

export interface AutomationDefinitionNodeUnzip {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeUnzipConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeUnzipConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination?: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeUnzipConfigOnError[];
    overwriteFiles?: boolean;
}

export interface AutomationDefinitionNodeUnzipConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeWait {
    /**
     * Configuration fields for this node type.
     */
    config?: outputs.AutomationDefinitionNodeWaitConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeWaitConfig {
    delaySeconds?: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeWaitConfigOnError[];
}

export interface AutomationDefinitionNodeWaitConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface AutomationDefinitionNodeZip {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.AutomationDefinitionNodeZipConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return?: boolean;
}

export interface AutomationDefinitionNodeZipConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors?: outputs.AutomationDefinitionNodeZipConfigOnError[];
    overwriteFiles?: boolean;
}

export interface AutomationDefinitionNodeZipConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinition {
    /**
     * Directed connections between nodes.
     */
    edges: outputs.GetAutomationDefinitionEdge[];
    /**
     * Flat list of nodes. Every node requires a unique id and a type, accepts the node-specific config fields documented below, and action or control-flow nodes may set return to true to include their output in the Automation result.
     */
    nodes: outputs.GetAutomationDefinitionNode[];
    /**
     * Automation definition schema version.
     */
    schemaVersion: number;
}

export interface GetAutomationDefinitionEdge {
    /**
     * Source node ID.
     */
    from: string;
    /**
     * Named destination input. Omit for the main input.
     */
    input: string;
    /**
     * Named source output. Omit for the main output.
     */
    output: string;
    /**
     * Destination node ID.
     */
    to: string;
}

export interface GetAutomationDefinitionNode {
    agentCompute: outputs.GetAutomationDefinitionNodeAgentCompute;
    aggregate: outputs.GetAutomationDefinitionNodeAggregate;
    as2Send: outputs.GetAutomationDefinitionNodeAs2Send;
    copyFile: outputs.GetAutomationDefinitionNodeCopyFile;
    createFolder: outputs.GetAutomationDefinitionNodeCreateFolder;
    deleteFile: outputs.GetAutomationDefinitionNodeDeleteFile;
    documentConvert: outputs.GetAutomationDefinitionNodeDocumentConvert;
    extract: outputs.GetAutomationDefinitionNodeExtract;
    filter: outputs.GetAutomationDefinitionNodeFilter;
    gpgDecrypt: outputs.GetAutomationDefinitionNodeGpgDecrypt;
    gpgEncrypt: outputs.GetAutomationDefinitionNodeGpgEncrypt;
    if: outputs.GetAutomationDefinitionNodeIf;
    imageConvert: outputs.GetAutomationDefinitionNodeImageConvert;
    importFile: outputs.GetAutomationDefinitionNodeImportFile;
    join: outputs.GetAutomationDefinitionNodeJoin;
    moveFile: outputs.GetAutomationDefinitionNodeMoveFile;
    runAutomation: outputs.GetAutomationDefinitionNodeRunAutomation;
    runSync: outputs.GetAutomationDefinitionNodeRunSync;
    sendEmail: outputs.GetAutomationDefinitionNodeSendEmail;
    setMetadata: outputs.GetAutomationDefinitionNodeSetMetadata;
    switch: outputs.GetAutomationDefinitionNodeSwitch;
    transform: outputs.GetAutomationDefinitionNodeTransform;
    triggerAction: outputs.GetAutomationDefinitionNodeTriggerAction;
    triggerEmail: outputs.GetAutomationDefinitionNodeTriggerEmail;
    triggerManual: outputs.GetAutomationDefinitionNodeTriggerManual;
    triggerScheduled: outputs.GetAutomationDefinitionNodeTriggerScheduled;
    triggerWebhook: outputs.GetAutomationDefinitionNodeTriggerWebhook;
    unzip: outputs.GetAutomationDefinitionNodeUnzip;
    wait: outputs.GetAutomationDefinitionNodeWait;
    zip: outputs.GetAutomationDefinitionNodeZip;
}

export interface GetAutomationDefinitionNodeAgentCompute {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeAgentComputeConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeAgentComputeConfig {
    arguments: {[key: string]: string};
    command: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeAgentComputeConfigOnError[];
    remoteServerId: number;
}

export interface GetAutomationDefinitionNodeAgentComputeConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeAggregate {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeAggregateConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeAggregateConfig {
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeAggregateConfigOnError[];
}

export interface GetAutomationDefinitionNodeAggregateConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeAs2Send {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeAs2SendConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeAs2SendConfig {
    as2PartnerId: number;
    as2StationId: number;
    as2Subject: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeAs2SendConfigOnError[];
}

export interface GetAutomationDefinitionNodeAs2SendConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeCopyFile {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeCopyFileConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeCopyFileConfig {
    alwaysOverwriteSizeMatchingFiles: boolean;
    destinationReplaceFrom: string;
    destinationReplaceTo: string;
    /**
     * One or more destination path templates.
     */
    destinations: string[];
    excludePattern: string;
    flattenDestinationStructure: boolean;
    ignoreLockedFolders: boolean;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeCopyFileConfigOnError[];
    overwriteFiles: boolean;
}

export interface GetAutomationDefinitionNodeCopyFileConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeCreateFolder {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeCreateFolderConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeCreateFolderConfig {
    /**
     * One or more destination path templates.
     */
    destinations: string[];
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeCreateFolderConfigOnError[];
}

export interface GetAutomationDefinitionNodeCreateFolderConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeDeleteFile {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeDeleteFileConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeDeleteFileConfig {
    excludePattern: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeDeleteFileConfigOnError[];
}

export interface GetAutomationDefinitionNodeDeleteFileConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeDocumentConvert {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeDocumentConvertConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeDocumentConvertConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeDocumentConvertConfigOnError[];
    overwriteFiles: boolean;
    targetFormat: string;
}

export interface GetAutomationDefinitionNodeDocumentConvertConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeExtract {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeExtractConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeExtractConfig {
    contentMode: string;
    includeContent: boolean;
    includeMetadata: boolean;
    maxChars: number;
    maxPages: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeExtractConfigOnError[];
}

export interface GetAutomationDefinitionNodeExtractConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeFilter {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeFilterConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeFilterConfig {
    /**
     * A bare Files TransformScript expression, no braces.
     */
    condition: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeFilterConfigOnError[];
}

export interface GetAutomationDefinitionNodeFilterConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeGpgDecrypt {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeGpgDecryptConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeGpgDecryptConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination: string;
    gpgKeyId: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeGpgDecryptConfigOnError[];
    overwriteFiles: boolean;
}

export interface GetAutomationDefinitionNodeGpgDecryptConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeGpgEncrypt {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeGpgEncryptConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeGpgEncryptConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination: string;
    gpgKeyId: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeGpgEncryptConfigOnError[];
    overwriteFiles: boolean;
}

export interface GetAutomationDefinitionNodeGpgEncryptConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeIf {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeIfConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeIfConfig {
    /**
     * A bare Files TransformScript expression, no braces.
     */
    condition: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeIfConfigOnError[];
}

export interface GetAutomationDefinitionNodeIfConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeImageConvert {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeImageConvertConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeImageConvertConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination: string;
    height: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeImageConvertConfigOnError[];
    overwriteFiles: boolean;
    targetFormat: string;
    width: number;
}

export interface GetAutomationDefinitionNodeImageConvertConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeImportFile {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeImportFileConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeImportFileConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    content: string;
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination: string;
    headers: {[key: string]: string};
    method: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeImportFileConfigOnError[];
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    url: string;
}

export interface GetAutomationDefinitionNodeImportFileConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeJoin {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeJoinConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeJoinConfig {
    joinType: string;
    /**
     * A bare Files TransformScript expression, no braces.
     */
    leftKey: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeJoinConfigOnError[];
    /**
     * A bare Files TransformScript expression, no braces.
     */
    rightKey: string;
}

export interface GetAutomationDefinitionNodeJoinConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeMoveFile {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeMoveFileConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeMoveFileConfig {
    destinationReplaceFrom: string;
    destinationReplaceTo: string;
    /**
     * One or more destination path templates.
     */
    destinations: string[];
    excludePattern: string;
    flattenDestinationStructure: boolean;
    ignoreLockedFolders: boolean;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeMoveFileConfigOnError[];
    overwriteFiles: boolean;
}

export interface GetAutomationDefinitionNodeMoveFileConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeRunAutomation {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeRunAutomationConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeRunAutomationConfig {
    automationId: number;
    automationVersion: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeRunAutomationConfigOnError[];
}

export interface GetAutomationDefinitionNodeRunAutomationConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeRunSync {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeRunSyncConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeRunSyncConfig {
    legacySyncIds: number[];
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeRunSyncConfigOnError[];
    syncIds: number[];
}

export interface GetAutomationDefinitionNodeRunSyncConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeSendEmail {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeSendEmailConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeSendEmailConfig {
    attachments: string[];
    bccs: string[];
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    body: string;
    ccs: string[];
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    from: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeSendEmailConfigOnError[];
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    subject: string;
    tos: string[];
}

export interface GetAutomationDefinitionNodeSendEmailConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeSetMetadata {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeSetMetadataConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeSetMetadataConfig {
    metadata: {[key: string]: string};
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeSetMetadataConfigOnError[];
}

export interface GetAutomationDefinitionNodeSetMetadataConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeSwitch {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeSwitchConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeSwitchConfig {
    /**
     * Switch cases. Each item contains a unique output name and the condition that sends items to it.
     */
    cases: outputs.GetAutomationDefinitionNodeSwitchConfigCase[];
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeSwitchConfigOnError[];
}

export interface GetAutomationDefinitionNodeSwitchConfigCase {
    /**
     * A bare Files TransformScript expression, no braces.
     */
    condition: string;
    /**
     * Identifier used to connect nodes and named inputs or outputs.
     */
    name: string;
}

export interface GetAutomationDefinitionNodeSwitchConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeTransform {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeTransformConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeTransformConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination: string;
    fts: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeTransformConfigOnError[];
}

export interface GetAutomationDefinitionNodeTransformConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeTriggerAction {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeTriggerActionConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface GetAutomationDefinitionNodeTriggerActionConfig {
    excludePattern: string;
    path: string;
    pathTimeZone: string;
    source: string;
    triggerActions: string[];
}

export interface GetAutomationDefinitionNodeTriggerEmail {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeTriggerEmailConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface GetAutomationDefinitionNodeTriggerEmailConfig {
    allowedSenders: string[];
    excludePattern: string;
    limit: number;
    pathTimeZone: string;
    saveBody: boolean;
    source: string;
}

export interface GetAutomationDefinitionNodeTriggerManual {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeTriggerManualConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface GetAutomationDefinitionNodeTriggerManualConfig {
    excludePattern: string;
    limit: number;
    path: string;
    pathTimeZone: string;
    source: string;
}

export interface GetAutomationDefinitionNodeTriggerScheduled {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeTriggerScheduledConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface GetAutomationDefinitionNodeTriggerScheduledConfig {
    excludePattern: string;
    holidayRegion: string;
    interval: string;
    limit: number;
    path: string;
    pathTimeZone: string;
    recurringDay: number;
    recurringDays: number[];
    scheduleDaysOfWeeks: number[];
    scheduleId: number;
    scheduleTimeZone: string;
    scheduleTimesOfDays: string[];
    source: string;
}

export interface GetAutomationDefinitionNodeTriggerWebhook {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeTriggerWebhookConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
}

export interface GetAutomationDefinitionNodeTriggerWebhookConfig {
    excludePattern: string;
    limit: number;
    path: string;
    pathTimeZone: string;
    source: string;
}

export interface GetAutomationDefinitionNodeUnzip {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeUnzipConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeUnzipConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeUnzipConfigOnError[];
    overwriteFiles: boolean;
}

export interface GetAutomationDefinitionNodeUnzipConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeWait {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeWaitConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeWaitConfig {
    delaySeconds: number;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeWaitConfigOnError[];
}

export interface GetAutomationDefinitionNodeWaitConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetAutomationDefinitionNodeZip {
    /**
     * Configuration fields for this node type.
     */
    config: outputs.GetAutomationDefinitionNodeZipConfig;
    /**
     * Unique node ID within the graph.
     */
    id: string;
    /**
     * Whether this node's output is included in the Automation result.
     */
    return: boolean;
}

export interface GetAutomationDefinitionNodeZipConfig {
    /**
     * A string value. Supports {{ fts }} interpolation, evaluated per item at execution time.
     */
    destination: string;
    /**
     * Error handlers. Each item selects an error pattern and whether to continue or propagate it.
     */
    onErrors: outputs.GetAutomationDefinitionNodeZipConfigOnError[];
    overwriteFiles: boolean;
}

export interface GetAutomationDefinitionNodeZipConfigOnError {
    action: string;
    /**
     * A typed error family (not-found, processing-failure, service-unavailable), family/specific-error, or * for all.
     */
    error: string;
}

export interface GetHolidayCalendarDefinition {
    /**
     * Keys 1 through 12 contain rules for that month. Key 0 contains calculated-date rules. At most 366 rules are allowed across all months.
     */
    months: {[key: string]: outputs.GetHolidayCalendarDefinitionMonths};
}

export interface GetHolidayCalendarDefinitionMonths {
    calculatedRules: outputs.GetHolidayCalendarDefinitionMonthsCalculatedRule[];
    fixedRules: outputs.GetHolidayCalendarDefinitionMonthsFixedRule[];
    weekdayRules: outputs.GetHolidayCalendarDefinitionMonthsWeekdayRule[];
}

export interface GetHolidayCalendarDefinitionMonthsCalculatedRule {
    /**
     * Exclusive end of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be later than start_time.
     */
    endTime: string;
    /**
     * Supported calculated-date function.
     */
    function: string;
    /**
     * Number of days to add to or subtract from the calculated date.
     */
    functionModifier: number;
    /**
     * Optional rule name.
     */
    name: string;
    /**
     * Optional function used to move the selected date when it falls on a weekend.
     */
    observed: string;
    /**
     * Inclusive start of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be earlier than end_time.
     */
    startTime: string;
    /**
     * Optional inclusive year restriction.
     */
    yearRanges: outputs.GetHolidayCalendarDefinitionMonthsCalculatedRuleYearRanges;
}

export interface GetHolidayCalendarDefinitionMonthsCalculatedRuleYearRanges {
    /**
     * Required start and end years.
     */
    between: outputs.GetHolidayCalendarDefinitionMonthsCalculatedRuleYearRangesBetween;
    /**
     * First year in which the rule applies.
     */
    from: number;
    /**
     * Years in which the rule applies.
     */
    limiteds: number[];
    /**
     * Last year in which the rule applies.
     */
    until: number;
}

export interface GetHolidayCalendarDefinitionMonthsCalculatedRuleYearRangesBetween {
    end: number;
    start: number;
}

export interface GetHolidayCalendarDefinitionMonthsFixedRule {
    /**
     * Exclusive end of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be later than start_time.
     */
    endTime: string;
    /**
     * Day of the month. Must be valid for the selected month.
     */
    mday: number;
    /**
     * Optional rule name.
     */
    name: string;
    /**
     * Optional function used to move the selected date when it falls on a weekend.
     */
    observed: string;
    /**
     * Inclusive start of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be earlier than end_time.
     */
    startTime: string;
    /**
     * Optional inclusive year restriction.
     */
    yearRanges: outputs.GetHolidayCalendarDefinitionMonthsFixedRuleYearRanges;
}

export interface GetHolidayCalendarDefinitionMonthsFixedRuleYearRanges {
    /**
     * Required start and end years.
     */
    between: outputs.GetHolidayCalendarDefinitionMonthsFixedRuleYearRangesBetween;
    /**
     * First year in which the rule applies.
     */
    from: number;
    /**
     * Years in which the rule applies.
     */
    limiteds: number[];
    /**
     * Last year in which the rule applies.
     */
    until: number;
}

export interface GetHolidayCalendarDefinitionMonthsFixedRuleYearRangesBetween {
    end: number;
    start: number;
}

export interface GetHolidayCalendarDefinitionMonthsWeekdayRule {
    /**
     * Exclusive end of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be later than start_time.
     */
    endTime: string;
    /**
     * Optional rule name.
     */
    name: string;
    /**
     * Optional function used to move the selected date when it falls on a weekend.
     */
    observed: string;
    /**
     * Inclusive start of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be earlier than end_time.
     */
    startTime: string;
    /**
     * Day of the week, where 0 is Sunday and 6 is Saturday.
     */
    wday: number;
    /**
     * Occurrence of the weekday in the month. Negative values count from the end of the month.
     */
    week: number;
    /**
     * Optional inclusive year restriction.
     */
    yearRanges: outputs.GetHolidayCalendarDefinitionMonthsWeekdayRuleYearRanges;
}

export interface GetHolidayCalendarDefinitionMonthsWeekdayRuleYearRanges {
    /**
     * Required start and end years.
     */
    between: outputs.GetHolidayCalendarDefinitionMonthsWeekdayRuleYearRangesBetween;
    /**
     * First year in which the rule applies.
     */
    from: number;
    /**
     * Years in which the rule applies.
     */
    limiteds: number[];
    /**
     * Last year in which the rule applies.
     */
    until: number;
}

export interface GetHolidayCalendarDefinitionMonthsWeekdayRuleYearRangesBetween {
    end: number;
    start: number;
}

export interface HolidayCalendarDefinition {
    /**
     * Keys 1 through 12 contain rules for that month. Key 0 contains calculated-date rules. At most 366 rules are allowed across all months.
     */
    months: {[key: string]: outputs.HolidayCalendarDefinitionMonths};
}

export interface HolidayCalendarDefinitionMonths {
    calculatedRules?: outputs.HolidayCalendarDefinitionMonthsCalculatedRule[];
    fixedRules?: outputs.HolidayCalendarDefinitionMonthsFixedRule[];
    weekdayRules?: outputs.HolidayCalendarDefinitionMonthsWeekdayRule[];
}

export interface HolidayCalendarDefinitionMonthsCalculatedRule {
    /**
     * Exclusive end of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be later than start_time.
     */
    endTime?: string;
    /**
     * Supported calculated-date function.
     */
    function: string;
    /**
     * Number of days to add to or subtract from the calculated date.
     */
    functionModifier?: number;
    /**
     * Optional rule name.
     */
    name?: string;
    /**
     * Optional function used to move the selected date when it falls on a weekend.
     */
    observed?: string;
    /**
     * Inclusive start of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be earlier than end_time.
     */
    startTime?: string;
    /**
     * Optional inclusive year restriction.
     */
    yearRanges?: outputs.HolidayCalendarDefinitionMonthsCalculatedRuleYearRanges;
}

export interface HolidayCalendarDefinitionMonthsCalculatedRuleYearRanges {
    /**
     * Required start and end years.
     */
    between?: outputs.HolidayCalendarDefinitionMonthsCalculatedRuleYearRangesBetween;
    /**
     * First year in which the rule applies.
     */
    from?: number;
    /**
     * Years in which the rule applies.
     */
    limiteds?: number[];
    /**
     * Last year in which the rule applies.
     */
    until?: number;
}

export interface HolidayCalendarDefinitionMonthsCalculatedRuleYearRangesBetween {
    end: number;
    start: number;
}

export interface HolidayCalendarDefinitionMonthsFixedRule {
    /**
     * Exclusive end of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be later than start_time.
     */
    endTime?: string;
    /**
     * Day of the month. Must be valid for the selected month.
     */
    mday: number;
    /**
     * Optional rule name.
     */
    name?: string;
    /**
     * Optional function used to move the selected date when it falls on a weekend.
     */
    observed?: string;
    /**
     * Inclusive start of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be earlier than end_time.
     */
    startTime?: string;
    /**
     * Optional inclusive year restriction.
     */
    yearRanges?: outputs.HolidayCalendarDefinitionMonthsFixedRuleYearRanges;
}

export interface HolidayCalendarDefinitionMonthsFixedRuleYearRanges {
    /**
     * Required start and end years.
     */
    between?: outputs.HolidayCalendarDefinitionMonthsFixedRuleYearRangesBetween;
    /**
     * First year in which the rule applies.
     */
    from?: number;
    /**
     * Years in which the rule applies.
     */
    limiteds?: number[];
    /**
     * Last year in which the rule applies.
     */
    until?: number;
}

export interface HolidayCalendarDefinitionMonthsFixedRuleYearRangesBetween {
    end: number;
    start: number;
}

export interface HolidayCalendarDefinitionMonthsWeekdayRule {
    /**
     * Exclusive end of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be later than start_time.
     */
    endTime?: string;
    /**
     * Optional rule name.
     */
    name?: string;
    /**
     * Optional function used to move the selected date when it falls on a weekend.
     */
    observed?: string;
    /**
     * Inclusive start of a partial-day window in 24-hour HH:MM or HH:MM:SS format. Must be earlier than end_time.
     */
    startTime?: string;
    /**
     * Day of the week, where 0 is Sunday and 6 is Saturday.
     */
    wday: number;
    /**
     * Occurrence of the weekday in the month. Negative values count from the end of the month.
     */
    week: number;
    /**
     * Optional inclusive year restriction.
     */
    yearRanges?: outputs.HolidayCalendarDefinitionMonthsWeekdayRuleYearRanges;
}

export interface HolidayCalendarDefinitionMonthsWeekdayRuleYearRanges {
    /**
     * Required start and end years.
     */
    between?: outputs.HolidayCalendarDefinitionMonthsWeekdayRuleYearRangesBetween;
    /**
     * First year in which the rule applies.
     */
    from?: number;
    /**
     * Years in which the rule applies.
     */
    limiteds?: number[];
    /**
     * Last year in which the rule applies.
     */
    until?: number;
}

export interface HolidayCalendarDefinitionMonthsWeekdayRuleYearRangesBetween {
    end: number;
    start: number;
}

