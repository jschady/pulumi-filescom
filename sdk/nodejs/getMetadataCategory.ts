// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * A MetadataCategory defines a reusable set of Custom Metadata rules that can be assigned to folders
 *
 * via a folder behavior. Each category specifies named metadata keys with optional allowed-value
 *
 * constraints, and a set of default columns to display in the UI.
 *
 * If a key's <span pulumi-lang-nodejs="`allowedValues`" pulumi-lang-dotnet="`AllowedValues`" pulumi-lang-go="`allowedValues`" pulumi-lang-python="`allowed_values`" pulumi-lang-yaml="`allowedValues`" pulumi-lang-java="`allowedValues`" pulumi-lang-hcl="`allowed_values`">`allowedValues`</span> array is empty, it is treated as a free-form text field.
 *
 * If the array is non-empty, the key is constrained to those values in the Web UI.
 *
 * ## Example Usage
 *
 * <!--Start PulumiCodeChooser -->
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as filescom from "pulumi-filescom";
 *
 * const exampleMetadataCategory = filescom.getMetadataCategory({
 *     id: 1,
 * });
 * ```
 * <!--End PulumiCodeChooser -->
 */
export function getMetadataCategory(args: GetMetadataCategoryArgs, opts?: pulumi.InvokeOptions): Promise<GetMetadataCategoryResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("filescom:index/getMetadataCategory:getMetadataCategory", {
        "id": args.id,
    }, opts);
}

/**
 * A collection of arguments for invoking getMetadataCategory.
 */
export interface GetMetadataCategoryArgs {
    /**
     * Metadata Category ID
     */
    id: number;
}

/**
 * A collection of values returned by getMetadataCategory.
 */
export interface GetMetadataCategoryResult {
    /**
     * Metadata keys that should appear as columns in the UI by default.
     */
    readonly defaultColumns: string[];
    /**
     * Map of key names to arrays of allowed values. An empty array means free-form text.
     */
    readonly definitions: {[key: string]: string[]};
    /**
     * Metadata Category ID
     */
    readonly id: number;
    /**
     * Name of the metadata category.
     */
    readonly name: string;
}
/**
 * A MetadataCategory defines a reusable set of Custom Metadata rules that can be assigned to folders
 *
 * via a folder behavior. Each category specifies named metadata keys with optional allowed-value
 *
 * constraints, and a set of default columns to display in the UI.
 *
 * If a key's <span pulumi-lang-nodejs="`allowedValues`" pulumi-lang-dotnet="`AllowedValues`" pulumi-lang-go="`allowedValues`" pulumi-lang-python="`allowed_values`" pulumi-lang-yaml="`allowedValues`" pulumi-lang-java="`allowedValues`" pulumi-lang-hcl="`allowed_values`">`allowedValues`</span> array is empty, it is treated as a free-form text field.
 *
 * If the array is non-empty, the key is constrained to those values in the Web UI.
 *
 * ## Example Usage
 *
 * <!--Start PulumiCodeChooser -->
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as filescom from "pulumi-filescom";
 *
 * const exampleMetadataCategory = filescom.getMetadataCategory({
 *     id: 1,
 * });
 * ```
 * <!--End PulumiCodeChooser -->
 */
export function getMetadataCategoryOutput(args: GetMetadataCategoryOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetMetadataCategoryResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("filescom:index/getMetadataCategory:getMetadataCategory", {
        "id": args.id,
    }, opts);
}

/**
 * A collection of arguments for invoking getMetadataCategory.
 */
export interface GetMetadataCategoryOutputArgs {
    /**
     * Metadata Category ID
     */
    id: pulumi.Input<number>;
}
