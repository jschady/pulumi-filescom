// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * A MessageComment is a comment made by a user on a message.
 *
 * ## Example Usage
 *
 * <!--Start PulumiCodeChooser -->
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as filescom from "pulumi-filescom";
 *
 * const exampleMessageComment = new filescom.MessageComment("example_message_comment", {
 *     userId: 1,
 *     body: "What a great idea, thank you!",
 * });
 * ```
 * <!--End PulumiCodeChooser -->
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * Message Comments can be imported by specifying the id.
 *
 * ```sh
 * $ pulumi import filescom:index/messageComment:MessageComment example_message_comment 1
 * ```
 */
export class MessageComment extends pulumi.CustomResource {
    /**
     * Get an existing MessageComment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: MessageCommentState, opts?: pulumi.CustomResourceOptions): MessageComment {
        return new MessageComment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'filescom:index/messageComment:MessageComment';

    /**
     * Returns true if the given object is an instance of MessageComment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is MessageComment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === MessageComment.__pulumiType;
    }

    /**
     * Comment body.
     */
    declare public readonly body: pulumi.Output<string>;
    /**
     * Reactions to this comment.
     */
    declare public /*out*/ readonly reactions: pulumi.Output<any>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * User ID.  Provide a value of <span pulumi-lang-nodejs="`0`" pulumi-lang-dotnet="`0`" pulumi-lang-go="`0`" pulumi-lang-python="`0`" pulumi-lang-yaml="`0`" pulumi-lang-java="`0`" pulumi-lang-hcl="`0`">`0`</span> to operate the current session's user.
     */
    declare public readonly userId: pulumi.Output<number | undefined>;

    /**
     * Create a MessageComment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: MessageCommentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: MessageCommentArgs | MessageCommentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as MessageCommentState | undefined;
            resourceInputs["body"] = state?.body;
            resourceInputs["reactions"] = state?.reactions;
            resourceInputs["userId"] = state?.userId;
        } else {
            const args = argsOrState as MessageCommentArgs | undefined;
            if (args?.body === undefined && !opts.urn) {
                throw new Error("Missing required property 'body'");
            }
            resourceInputs["body"] = args?.body;
            resourceInputs["userId"] = args?.userId ? pulumi.secret(args.userId) : undefined;
            resourceInputs["reactions"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["userId"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(MessageComment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering MessageComment resources.
 */
export interface MessageCommentState {
    /**
     * Comment body.
     */
    body?: pulumi.Input<string | undefined>;
    /**
     * Reactions to this comment.
     */
    reactions?: any | undefined;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * User ID.  Provide a value of <span pulumi-lang-nodejs="`0`" pulumi-lang-dotnet="`0`" pulumi-lang-go="`0`" pulumi-lang-python="`0`" pulumi-lang-yaml="`0`" pulumi-lang-java="`0`" pulumi-lang-hcl="`0`">`0`</span> to operate the current session's user.
     */
    userId?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a MessageComment resource.
 */
export interface MessageCommentArgs {
    /**
     * Comment body.
     */
    body: pulumi.Input<string>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * User ID.  Provide a value of <span pulumi-lang-nodejs="`0`" pulumi-lang-dotnet="`0`" pulumi-lang-go="`0`" pulumi-lang-python="`0`" pulumi-lang-yaml="`0`" pulumi-lang-java="`0`" pulumi-lang-hcl="`0`">`0`</span> to operate the current session's user.
     */
    userId?: pulumi.Input<number | undefined>;
}
