// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * A MetadataCategory defines a reusable set of Custom Metadata rules that can be assigned to folders
 *
 * via a folder behavior. Each category specifies named metadata keys with optional allowed-value
 *
 * constraints, and a set of default columns to display in the UI.
 *
 * If a key's <span pulumi-lang-nodejs="`allowedValues`" pulumi-lang-dotnet="`AllowedValues`" pulumi-lang-go="`allowedValues`" pulumi-lang-python="`allowed_values`" pulumi-lang-yaml="`allowedValues`" pulumi-lang-java="`allowedValues`" pulumi-lang-hcl="`allowed_values`">`allowedValues`</span> array is empty, it is treated as a free-form text field.
 *
 * If the array is non-empty, the key is constrained to those values in the Web UI.
 *
 * ## Example Usage
 *
 * <!--Start PulumiCodeChooser -->
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as filescom from "pulumi-filescom";
 *
 * const exampleMetadataCategory = new filescom.MetadataCategory("example_metadata_category", {
 *     name: "Approval Workflow",
 *     defaultColumns: ["Approval Status"],
 * });
 * ```
 * <!--End PulumiCodeChooser -->
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * Metadata Categories can be imported by specifying the id.
 *
 * ```sh
 * $ pulumi import filescom:index/metadataCategory:MetadataCategory example_metadata_category 1
 * ```
 */
export class MetadataCategory extends pulumi.CustomResource {
    /**
     * Get an existing MetadataCategory resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: MetadataCategoryState, opts?: pulumi.CustomResourceOptions): MetadataCategory {
        return new MetadataCategory(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'filescom:index/metadataCategory:MetadataCategory';

    /**
     * Returns true if the given object is an instance of MetadataCategory.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is MetadataCategory {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === MetadataCategory.__pulumiType;
    }

    /**
     * Metadata keys that should appear as columns in the UI by default.
     */
    declare public readonly defaultColumns: pulumi.Output<string[]>;
    /**
     * Map of key names to arrays of allowed values. An empty array means free-form text.
     */
    declare public /*out*/ readonly definitions: pulumi.Output<{[key: string]: string[]}>;
    /**
     * Name of the metadata category.
     */
    declare public readonly name: pulumi.Output<string>;

    /**
     * Create a MetadataCategory resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: MetadataCategoryArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: MetadataCategoryArgs | MetadataCategoryState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as MetadataCategoryState | undefined;
            resourceInputs["defaultColumns"] = state?.defaultColumns;
            resourceInputs["definitions"] = state?.definitions;
            resourceInputs["name"] = state?.name;
        } else {
            const args = argsOrState as MetadataCategoryArgs | undefined;
            resourceInputs["defaultColumns"] = args?.defaultColumns;
            resourceInputs["name"] = args?.name;
            resourceInputs["definitions"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(MetadataCategory.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering MetadataCategory resources.
 */
export interface MetadataCategoryState {
    /**
     * Metadata keys that should appear as columns in the UI by default.
     */
    defaultColumns?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Map of key names to arrays of allowed values. An empty array means free-form text.
     */
    definitions?: pulumi.Input<{[key: string]: pulumi.Input<pulumi.Input<string>[]>} | undefined>;
    /**
     * Name of the metadata category.
     */
    name?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a MetadataCategory resource.
 */
export interface MetadataCategoryArgs {
    /**
     * Metadata keys that should appear as columns in the UI by default.
     */
    defaultColumns?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Name of the metadata category.
     */
    name?: pulumi.Input<string | undefined>;
}
